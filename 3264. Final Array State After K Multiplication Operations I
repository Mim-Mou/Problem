class Solution:
    def getFinalState(self, nums: List[int], k: int, m: int) -> List[int]:
        for i in range(k):
            a=nums.index(min(nums))
            nums[a]*=m
        return nums        
